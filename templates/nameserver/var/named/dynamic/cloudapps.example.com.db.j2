$ORIGIN .
$TTL 1 ; 1 seconds (for testing only)
cloudapps.{{ openshift_domain }}       IN SOA  ns1.cloudapps.{{ openshift_domain }}. hostmaster.cloudapps.{{ openshift_domain }}. (
            2011112904 ; serial
            60         ; refresh (1 minute)
            15         ; retry (15 seconds)
            1800       ; expire (30 minutes)
            10         ; minimum (10 seconds)
            )
        NS  ns1.cloudapps.{{ openshift_domain }}.
        MX  10 mail.cloudapps.{{ openshift_domain }}.
$ORIGIN cloudapps.{{ openshift_domain }}.
*           A   {{ router_public_ip }}
